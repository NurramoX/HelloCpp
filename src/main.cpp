#include <iostream>
#include <string>

int main() {
    std::string  hello_world = "Hello, World";
    std::cout << hello_world << std::endl;
    std::cin.get();  // Keeps the window open until you hit Enter
    return 0;
}
